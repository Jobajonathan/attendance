export default function ActivitiesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-zinc-900">Activity Management</h1>
      <p className="mt-2 max-w-2xl text-sm text-zinc-600">
        Creating, opening, and closing Attendance and Message Review activities (PRD Section 4.3,
        FR-ATT-01 to 10) is built in Phase 2, on top of the <code>activities</code> table already
        in place. This placeholder confirms the Administrative Officer lands here on login per
        Section 6.2.
      </p>
    </div>
  );
}
